import { NextResponse } from 'next/server';
export async function POST() {
  // Placeholder: batch-validate rows against existing entities (fuzzy match, etc.)
  return NextResponse.json({ ok: true, issues: [] });
}